{{- if .Values.grafana.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ultra-logging-engine.fullname" . }}-grafana-dashboards
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ultra-logging-engine.labels" . | nindent 4 }}
    app.kubernetes.io/component: grafana
data:
  ultra-performance-analytics.json: |
{{ .Files.Get "grafana-dashboards/ultra-performance-analytics.json" | indent 4 }}
  
  log-search-analytics.json: |
{{ .Files.Get "grafana-dashboards/log-search-analytics.json" | indent 4 }}
  
  logging-performance.json: |
{{ .Files.Get "grafana-dashboards/logging-performance.json" | indent 4 }}
                  "steps": [
                    {"color": "green", "value": null},
                    {"color": "yellow", "value": 1},
                    {"color": "red", "value": 10}
                  ]
                }
              }
            },
            "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
          },
          {
            "id": 3,
            "title": "Trade Execution Logs",
            "type": "logs",
            "targets": [
              {
                "expr": "{component=\"trade-executor\"}"
              }
            ],
            "gridPos": {"h": 12, "w": 24, "x": 0, "y": 8}
          }
        ],
        "time": {
          "from": "now-1h",
          "to": "now"
        },
        "refresh": "5s"
      }
    }

  trading-performance.json: |
    {
      "dashboard": {
        "id": null,
        "title": "Trading Performance Metrics",
        "tags": ["trading", "performance", "latency"],
        "panels": [
          {
            "id": 1,
            "title": "Log Processing Latency",
            "type": "graph",
            "targets": [
              {
                "expr": "histogram_quantile(0.95, rate(ultra_logger_processing_duration_seconds_bucket[5m]))",
                "legendFormat": "95th percentile"
              },
              {
                "expr": "histogram_quantile(0.50, rate(ultra_logger_processing_duration_seconds_bucket[5m]))",
                "legendFormat": "50th percentile"
              }
            ],
            "yAxes": [
              {
                "unit": "s",
                "min": 0
              }
            ],
            "gridPos": {"h": 8, "w": 24, "x": 0, "y": 0}
          },
          {
            "id": 2,
            "title": "Memory Usage",
            "type": "graph",
            "targets": [
              {
                "expr": "ultra_logger_memory_usage_bytes",
                "legendFormat": "Memory Used"
              }
            ],
            "yAxes": [
              {
                "unit": "bytes"
              }
            ],
            "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8}
          },
          {
            "id": 3,
            "title": "Buffer Utilization",
            "type": "graph",
            "targets": [
              {
                "expr": "ultra_logger_buffer_utilization_percent",
                "legendFormat": "Buffer %"
              }
            ],
            "yAxes": [
              {
                "unit": "percent",
                "max": 100
              }
            ],
            "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8}
          }
        ],
        "time": {
          "from": "now-1h", 
          "to": "now"
        },
        "refresh": "10s"
      }
    }
{{- end }}
